--\c one_db;

--SET search_path TO api_0_0_1, base_0_0_1, public;
-- Select User(user_token TEXT, criteria JSONB) JSONB

-- Insert User(user_token TEXT, chelate JSON) JSONB
-- Insert User(user_token TEXT, keypattern TEXT, chelate JSON) JSONB
--                              ['username','const#USER','*']
-- Update User(user_token TEXT, primaryKey JSON, form JSON) JSONB

-- Update user_upd(user_token TEXT, chelate JSON) JSONB

-- Delete User(user_token TEXT, pk TEXT, sk TEXT) JSONB
